# Design Tokens

Generic color, typography, shape, and spacing tokens live in `:core:ui`.

Feature-specific components stay in the feature presentation module. For
example, a door-status card belongs to the presentation module of the feature
that owns door access, not to `:core:ui`.
